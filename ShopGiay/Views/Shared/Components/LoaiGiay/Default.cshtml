@model IEnumerable<ShopGiay.Models.Loaigiay>

@foreach (var item in Model)
{
    <li>
        <a class="dropdown-item"
           asp-controller="Mathangs"
           asp-action="TheoLoai"
           asp-route-id="@item.MaLg">
            @item.Ten
        </a>
    </li>
}
